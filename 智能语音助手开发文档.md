# 为什么选择websocket而不是http?

语音对话场景需要低延迟的双向实时通信，而 HTTP 本质上是“请求-响应”模型，必须由客户端先发起请求，服务端才能返回数据；SSE 虽然支持服务端推送，但只能单向通信。相比之下，WebSocket 采用长连接机制，支持客户端与服务端之间的**全双工（通信双方可同时双向发送数据）**实时通信，因此更适合语音助手、WebRTC 等实时交互场景。

# Command信令处理模块

- 使用**策略模式+工厂模式**：对于不同信令类型，设计不同的信令处理器，通过实现ComandHandler接口来实现并注册为Bean，通过CommandDispatcher工厂调用对口的信令处理器
- BaseCommandHandler抽象基类，实现公共方法，比如发送message给对应的会话；表现出java的多态

# Agent工具调用

核心流程：构建ToolDef（工具列表）–> 调用LLM规定不是必须要返回工具，根据用户需求来选择工具，返回工具选择 –> 解析返回message，获取tool_calls解析工具列表 –> 执行对应的工具 –> 将工具调用结果以 **role: "tool"** 消息追加到对话上下文 –> 再次调用LLM生成最终回答

使用的是OpenAI兼容的Function Calling协议，用Function Calling让LLM能主动调用外部系统功能工具，而不是在prompt中硬编码；且每次请求都要带上tools的定义，因为大模型不会记住你的工具有哪些，所以继续带上工具定义可以让ai知道这个结果是哪个工具返回的，这个工具结构是怎样的，提高回答的准确率	

# LLM大模型层

- 普通同步调用：使用RestTemplate.exchange发送POST请求，exchange方法是同步阻塞的，所以当请求发出后，当前线程会等待，直到LLM生成完毕返回response后才会有反应，用户需要等待，体验较差
- 流式调用：使用原生 **HttpURLConnection + SSE解析**,Restemplate默认会等待整个响应结束才会返回，不符合流式的要求，所以用SSE，SSE就是服务器会持续推送文本流，后端不断的读取行然后解析，每收到返回的token，就立刻**onToken.accept()**，直到遇到**[DONE]**，就自动结束
- ToolCalling：请求体中携带tools和tool_choice:"auto"，告诉模型有哪些工具可以用，让模型自行决定如何调用

### 为什么自己用HttpURLConnection而不是RestTemplate

因为RestTemplate是要等整个响应体生成完毕后才会返回，而SSE需要边接受边处理，是一个长期推送，

### 最终决定使用WebClient统一实现http请求

WebClient天然适配流式输出，使用WebClient可以保证一致性，让流式和非流式都用同一个http请求，而不是像之前一样**HttpURLConnection+RestTemplate**，两个技术栈较为老旧，spring官方不推荐restTemplate

### 流式输出给前端的时候，怎么保证pbx服务器语音合成输出和前端流式显示的是同步的呢

- 第一代：在流式输出的时候等token全部输出完，然后将完整的作为tts发给pbx
- 第二代：不再等完整的LLM输出，而是每生成一个完整的句子，就触发TTS_SENTENCE逐句发送给PBX，pbx逐句播放，结束时发送完成信号

# RAG检索增强生成层

RAG整体流程：ASR接收到信令后 –> 查询预处理 –> 向量检索 –> prompt构建 –> LLM调用 –> 返回结果

### 查询预处理过程

避免口语化问题导致向量检索相似度较低

1. 先接受用户的原始信息和历史对话提示词，然后构建系统优化提示词（包含去除口语化的规则和一些例子），交给大模型，要求大模型去除口语化，生成一到三个方案，严格通过json格式返回，将大模型的原始回复继续解析
2. 解析返回的json数据，拿到queries数组后将结果存放在list中；如果json解析失败，按行解析
3. 将结果返回给RAGQAService进行多个查询体查询优化

### 多查询体查询

用多个优化后的查询体去做多次向量检索，然后将结果合并，去重，排序，最终返回最相关的topk；提高召回率

### 如何判断是否使用RAG？

- 第一代：设置一个总开关，由用户决定是否打开RAG功能
- 第二代：设置多层判断：
  - 第一层RAG总开关：如果是false，则直接普通对话，否则进入下一层
  - 第二层意图判断：设置三种意图：【普通对话，rag，工具调用】，通过预设提示词检查query命中哪一个意图的关键字，如果没有命中，设置简短地大模型提示词交给大模型判断（兜底）
  - 最终按意图进行对话处理

### RAG评估体系

- 考题类：规定测试集中每道题目应该有哪些属性，比如query、期望文档来源、期望意图
- 每道考题的结果类：规定每道题目的结果返回什么，比如返回的文档来源、期望文档的实际排名、实际意图、是否被阈值拦截
- 全部考题结果统计：recall@1，recall@3、recall@5总的命中率，mrr（第一个正确文档排名的倒数的平均值）
- 单个考题测试：获取考题中query的实际判断意图 –> 获向量检索 –> 提取检索文档的来源，分数 –> 判断是否被阈值过滤，L2计算法是分数越小越好 –> 计算首个相关文档的位置；最后把所有考题数据统计一下得到结果

待解决：如果发现recall@1比例很小，要如何去提高呢？

优化分片策略，从根源上提高正确率

### 分片优化

1、一开始只设置了最大长度和overlap，没有设置最小长度，导致有的分片只有标题；

优化方案：设置最小长度，小于最小长度则进行合并

2、优化后的切片流程：按标题切章节Section –> Section内按段落分组 –> 按token budget聚合 –> 超长段落降级到句子切分 –> 句子仍然超长则字符兜底 –> 生成overlap –> Section内合并小chunk

### milvus必须把collection加载到内存才能进行操作

milvus必须把collection加载到内存才能进行操作，所以初始化milvusClient时可以先把milvus加载到内存中

# webRTC在前端的操作

1. 通过麦克风获取音频
2. 创建webRTC连接通道RTCpeerConnection
3. 挂上音频轨道
4. 信令交互，通过后端发送offer sdp，持续交换ICE直到找到合适路径，然后接受answer
5. 接受pbx的tts

